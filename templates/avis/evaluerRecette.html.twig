{% extends 'base.html.twig' %}

{% block body %}

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-12">
                <h1>Donner votre avis sur</h1>
                    <h4 class="font-weight-light font-italic mb-3">{{ recette.titre }}</h4>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                    <button type="submit" class="btn btn-primary mb-5">Envoyer</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
    {% endblock %}
